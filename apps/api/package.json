{
  "name": "@bibleio/api",
  "license": "MIT-0",
  "description": "Rust Bibleio API",
  "scripts": {
    "install": "cargo install --path=.",
    "dev": "cargo run",
    "build": "cargo build --release",
    "fix": "cargo fmt && cargo clippy --fix --allow-dirty"
  }
}
