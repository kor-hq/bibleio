{
  "name": "@bibleio/api",
  "license": "MIT-0",
  "description": "Rust Bibleio API",
  "scripts": {
    "dev": "cargo run",
    "build": "cargo build --release",
    "fix": "cargo fmt && cargo clippy --fix --allow-dirty"
  }
}
