---
import { getCollection } from "astro:content";

const items = await getCollection("previews").then((c) =>
	c.filter((item) => !item.data.directory),
);
return Astro.redirect("/playground/base/button");
---
