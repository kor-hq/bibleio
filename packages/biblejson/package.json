{
	"name": "@bibleio/biblejson",
	"license": "MIT-0",
	"description": "Another new formatting standard for scripture",
	"scripts": {
		"dev": "cargo run",
		"build": "cargo build --release",
		"fix": "cargo fmt && cargo clippy --fix --allow-dirty"
	}
}
